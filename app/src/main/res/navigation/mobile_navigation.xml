<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/mobile_navigation"
    app:startDestination="@+id/nav_home">

    <!-- Destinasi untuk BerandaFragment -->
    <fragment
        android:id="@+id/nav_home"
        android:name="com.example.infogempaappmanual.BerandaFragment"
        android:label="Beranda"
        tools:layout="@layout/fragment_beranda" />

    <!-- Destinasi untuk GempaDirasakanFragment -->
    <fragment
        android:id="@+id/nav_felt_earthquake"
        android:name="com.example.infogempaappmanual.GempaDirasakanFragment"

    android:label="Gempa Signifikan (M ≥ 5.0)"

    tools:layout="@layout/fragment_gempa_dirasakan" />

    <!-- Destinasi untuk TentangFragment -->
    <fragment
        android:id="@+id/nav_about"
        android:name="com.example.infogempaappmanual.TentangFragment"
        android:label="Tentang Aplikasi"
        tools:layout="@layout/fragment_tentang" />

</navigation>